apiVersion: v1
kind: Service
metadata:
  name: consumer
  namespace: keda
  labels:
    app: consumer
    strategy: keda
spec:
  type: ClusterIP
  selector:
    app: consumer
    strategy: keda
  ports:
    - name: http
      port: 8080
      targetPort: 8080
